name: Deploy

on:
  workflow_call:
    inputs:
      release-tag:
        required: true
        type: string
      version:
        required: true
        type: string
      is-prerelease:
        required: true
        type: string
      is-plain-version:
        required: true
        type: string

jobs:
  no_deployment:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: ':'
